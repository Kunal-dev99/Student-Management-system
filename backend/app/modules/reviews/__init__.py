"""Weekly review queue — the first feature built on the AI-as-a-layer package.

Deterministic core: scores every active student by risk signals and produces a candidate
list. The AI layer only picks top-N with a sentence each; the score table renders beside
the picks so the reader can verify.
"""
